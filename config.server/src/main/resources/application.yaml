spring:
  application:
    name: config.server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/kshitizkapoor45/hotel-management-config
          clone-on-start: true

server:
  port: 8095
